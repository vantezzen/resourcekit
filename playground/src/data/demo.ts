/**
 * The signed-in identity, hard-coded for the demo. In a real app these
 * come from your auth/session; here they're constants so the playground
 * stays about ResourceKit, not login screens.
 */
export const WORKSPACE_ID = "w1";
export const CURRENT_MEMBER_ID = "ada";
